# function gets coordinates from spatial data frame
# This function retrieves the coordinates from the spatial data frame of Toronto City Map.

get_coords <- function(x){
  s <- vector()
  for(i in 1:length(x@lines)){
    s <- rbind(s,x@lines[[i]]@Lines[[1]]@coords)
  
  }
  return(s)
}
